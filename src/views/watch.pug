extends base.pug

block content 
    p=video.description
    p=video.hashtags
    small=video.createdAt
    <br>
    a(href=`${video.id}/edit`) Edit Video &larr;
    <br>
    a(href=`${video.id}/delete`) Delete Video &larr;     