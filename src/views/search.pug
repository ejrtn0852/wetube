extends base.pug
include mixins/video

block content 
    form(method="GET")
        input(type="text" name="search" placeholder="Search by Video")
        input(type="submit" value="search")

    each video in videos 
        +video(video)